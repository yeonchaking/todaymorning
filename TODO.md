# TODO — 오늘도출근 (1.0 릴리즈 목표)

> **이 파일이 살아있는 백로그의 단일 출처(single source of truth)다.**
> 세션로그(`SESSION_LOG.md`)는 백로그를 보관하지 않는다. 각 세션은 종료 시
> 그 세션에서 **해결한 항목**과 **새로 추가한 항목**만 `[TODO 리스트 변경]`으로 기록하고,
> 그 변경을 이 파일에 반영한다. 운영 규칙은 `CLAUDE.md` "세션 운영" 참고.
>
> 목표: 아래 항목들을 채워 **Play Store 1.0 릴리즈**에 도달한다.

---

## 🎯 1.0 방향 (확정)

- **버스 + 지하철 모두 포함.** "지하철 미션 직접선택 화면"은 1.0 필수.
- **게임 요소 제외, 기능·실용 위주.** 포인트/레벨/배지/스트릭 강조형(게이미피케이션) 디자인은
  보류하고 `feature/gamification` 브랜치에 보존. main은 Claude Design v2(정보 카드형·신호등·그룹리스트)로 진행.
- 현재 v2 **UI는 구현됨**(메인/타임어택/설정). 아래 [0]은 그 UI에 **표시만 되고 실제 동작이 없는** 부분.

---

## 0. v2 UI 미구현 (화면엔 있으나 동작 없음 — 표시용 placeholder)

> 현재 화면에 노출돼 있어 사용자가 동작한다고 오해할 수 있는 항목. 우선 정리 대상.

- [ ] **TTS 음성 안내 (핵심)** — 막차/임박 시 "○○번 버스가 N분 후 도착 예정입니다" 음성 출력. 안내 시점(10·5·3분) on/off·저장, 타임어택 TTS 카드·설정 음성안내 그룹과 연동. 현재 화면 글자만 있고 TextToSpeech 연동 전무.
- [ ] **알람음 선택** — 설정 "알람음" 행. 음원 목록 선택·저장 + `AlarmRingService`가 사용.
- [ ] **진동 on/off 저장·제어** — 설정 진동 토글. 저장 + 알람 울림 시 진동 여부 반영.
- [x] ~~**홈 다음버스 실시간 미리보기**~~ — 해결(2026-06-20). 홈 "실시간 도착 정보 확인하기" 버튼 → 다이얼로그로 실제 도착정보 표시(노선/N분 후, 여러 건 화살표). 인라인 미리보기 대신 on-demand 조회 방식. 미션 미설정 시 버튼 숨김.

---

## 1. 기능 (Functional)

- [ ] **지하철 미션 직접선택 화면 (1.0 필수)** — 현재 버스만 UI 완성. 지하철은 모델·타임어택만 대응이라 직접선택 화면 신규 추가 필요(역명 검색 → 호선 선택). 설정 "지하철 추가" 행과 연결.
- [ ] **타임어택 신호등 정밀화** — 현재 "남은 시간"만으로 여유/곧출발/임박 판정. "다음 버스가 목표 시각 내 도착 가능한지"까지 반영하면 더 정확(막차 계산과 연동).
- [ ] **막차/첫차 계산 → 실시간 도착정보 반영** — 노선별 첫차·막차 시각을 가져와(버스: 공공API 노선정보 firstBusTm/lastBusTm, 지하철: 시간표) 현재 시각과 비교. 막차 끝난 상태면 도착정보 대신 "운행이 종료된 버스입니다" 같은 안내, 첫차 전이면 "아직 운행 전" 안내. 메인 실시간 도착정보 다이얼로그 + 타임어택 화면 모두 반영.
- [ ] **권한 온보딩 흐름** — 정확한 알람·전체화면 인텐트·알림 권한 미허용 시 핵심 기능 무력화. 현재 메인 배너 하나뿐 → 최초 실행 단계별 안내 필요.
- [ ] **오류·예외 처리 표준화** — 도착정보 없음 / 네트워크 실패 / 운행종료 시간대의 사용자 안내. HttpException 공통 매핑 유틸.
- [ ] **미사용 위치 권한 정리** — `ACCESS_FINE/COARSE_LOCATION`. 버스선택 현위치 이동에 실제 사용 중이므로 "기능 확정" 쪽으로 닫고 권한 사유서·개인정보방침에 반영.
- [ ] **API 키·한도 정책 확정** — 릴리즈 빌드에 공공 API 키 내장 + 일일 한도 존재. 1.0 범위를 "개인/소규모"로 명시할지 결정.
- [ ] **엣지케이스** — 미션 미설정 상태로 알람, 자정 넘김, 목표 시각 직전 진입 등.

---

## 2. UI

- [ ] **앱 이름 한글화** — `strings.xml`의 `app_name`이 "todaymorning"(영문 기본값) → "오늘도출근".
- [ ] **스플래시 화면** — 없음.
- [ ] **온보딩/첫 실행 화면** — 없음. 앱 목적·권한 설명·첫 미션 설정 유도.
- [ ] **상태별 UI 일관성 점검** — v2로 상당 개선됨. 로딩/빈/에러 잔여 점검(버스선택·결과 화면 등).
- [ ] **다듬기** — 다크모드 점검(v2 다크 토큰 실기기 확인), 폰트 스케일·접근성(contentDescription), 결과 화면 시각 정리. (Jua/Pretendard 등 폰트는 현재 시스템 폰트 — 적용 시 다운로더블/번들)

---

## 3. 제출 게이트 (Play Store)

> 기능/UI와 별개로, 없으면 업로드 자체가 막히는 항목.

- [ ] **릴리즈 서명 keystore** 생성 + `isMinifyEnabled`/난독화 확인.
- [ ] **개인정보처리방침 URL** — 알람·위치 권한 사용 시 필수.
- [ ] **민감 권한 선언서** — 정확한 알람 / 전체화면 인텐트 / 포그라운드 서비스 / 위치, Play Console에서 각각 사유 제출.
- [ ] **`foregroundServiceType=mediaPlayback` 적절성 재검토** — 실제 용도는 알람이라 심사에서 걸릴 수 있음.
- [ ] **스토어 등록정보** — 스크린샷, 설명, 피처 그래픽.

---

## 4. 기타 작업

> 간단한 작업·테스트·나중에 하기로 정한 것.

- [ ] OkHttp 로깅 레벨 BODY → BASIC 환원(릴리즈 전).
- [ ] **성공 시 자동실패 취소 도입 시 주의** — `cancelMissionFail()`은 현재 dead code이고, 자동실패 매일 반복 체인이 "성공한 날에도 MissionFailReceiver가 울려 다음날을 재등록"하는 데 의존한다. 향후 성공 시점에 `cancelMissionFail()`을 호출하도록 바꾸면, 그 자리에서 다음날 자동실패 재등록을 반드시 동반해야 체인이 안 끊긴다.
