# 잠재리스크 (오늘도출근)

> 지금 당장 할 작업은 아니지만, **나중에 코드를 바꿀 때 터질 수 있는 지뢰**를 기록하는 문서.
> TODO가 아니다 — 관련 코드를 건드리는 세션이 시작될 때 여기를 먼저 확인할 것.

---

## 1. 성공 시 자동실패 취소(`cancelMissionFail`) 도입 시 주의 (2026-07-06 기록)

- `cancelMissionFail()`은 현재 **dead code**다.
- 자동실패의 매일 반복 체인은 "성공한 날에도 `MissionFailReceiver`가 울려 다음날을 재등록"하는 데 의존한다.
- 향후 성공 시점에 `cancelMissionFail()`을 호출하도록 바꾸면, **그 자리에서 다음날 자동실패 재등록을 반드시 동반**해야 체인이 안 끊긴다.
- 2026-07-06 결정: 지금은 그대로 둔다 (코드 변경 없음).
