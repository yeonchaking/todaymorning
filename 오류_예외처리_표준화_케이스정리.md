# 오류·예외 처리 표준화 — 케이스 정리

> 2026-07-06 세션에서 조사·합의. **실제 코드 변경은 Phase 2로 미룸.** 이 문서는 다음 세션이
> 바로 착수할 수 있도록 현재 동작·구분해야 할 케이스·구현 방향을 남겨둔 것.

---

## 배경

`TODO.md` 1번 항목: "도착정보 없음 / 네트워크 실패 / 운행종료 시간대의 사용자 안내. HttpException
공통 매핑 유틸." 지금은 이 세 가지 상황이 전부 **같은 안내 문구**로 뭉뚱그려져 있어서,
사용자 입장에서 "내가 뭘 어떻게 해야 하는지" 구분이 안 된다.

## 현재 코드 동작 (as-is)

### `data/repository/TransitRepository.kt`
- `getBusArrivals()` (L20-39), `getSubwayArrivals()` (L119-143): API 호출 실패 시
  `catch (e: Exception) { emptyList() }` — **에러를 완전히 삼켜버림**. 호출부는 "그냥 0건"과
  "네트워크가 죽어서 0건"을 구분할 방법이 없다.
- `toTransitArrival()` (L146-155): 메시지에 `"운행종료"`/`"출발대기"`가 포함되면 그 차편을
  아예 `null` 처리해 리스트에서 사라짐 — "운행종료라서 없음"이라는 정보도 이 시점에 유실된다.
- 반면 `nearbyBusStops`/`searchBusStops`/`getRoutesAtStop`(설정 화면 쪽 API)은 이미
  `Log.e(...)` 로 실패를 로그에 남기고 있음 — 미션 폴링 경로보다 상태가 낫다.

### `mission/MissionEngine.kt` — `fetchArrivals()` (L159-199)
- `results.isEmpty()` 이면 무조건 `"도착 정보가 없습니다. (운행 종료 또는 API 키 미설정)"` 노출.
  → 진짜 운행종료인지, API 키가 비어있는지, 네트워크가 끊겼는지 셋 다 같은 문구.
- `catch (e: Exception)` 블록은 `"네트워크 오류: ${e.message}"`를 띄우긴 하지만, 위
  `TransitRepository`가 이미 예외를 삼켜 `emptyList()`로 바꿔버리므로 **이 catch가 실질적으로
  거의 발동하지 않는다** (DataStore 읽기 실패 등 극히 드문 경우만). 즉 지금 있는 매핑 코드가
  거의 죽은 코드나 다름없음.

## 구분해야 할 케이스

| 케이스 | 현재 신호 | 지금 사용자에게 보이는 것 |
|---|---|---|
| 정말로 도착 예정 차편이 없음 (정상) | `results.isEmpty()`, 예외 없음 | "도착 정보가 없습니다. (운행 종료 또는 API 키 미설정)" |
| API 키 미설정(`BuildConfig.BUS_API_KEY`/`SUBWAY_API_KEY` blank) | `getBusArrivals`/`getSubwayArrivals` 시작부터 `emptyList()` 즉시 반환 | 위와 동일 (구분 불가) |
| 네트워크 실패 (타임아웃, DNS, 연결 끊김 등 `IOException` 계열) | `TransitRepository` catch에서 `emptyList()`로 삼켜짐 | 위와 동일 (구분 불가) |
| 서버 응답 오류 (`retrofit2.HttpException`, 4xx/5xx) | 위와 동일하게 삼켜짐 | 위와 동일 (구분 불가) |
| 특정 차편이 운행종료 (`"운행종료"` 문자열) | `toTransitArrival()`에서 조용히 `null` | 그냥 리스트에서 빠짐. 전부 운행종료라 리스트가 비면 위 문구와 동일 |

## Phase 2 구현 방향

1. **에러 매핑 유틸 신설** — 예: `data/repository/ApiErrorMapper.kt`
   ```kotlin
   fun Throwable.toUserMessage(): String = when (this) {
       is retrofit2.HttpException -> "서버 응답 오류 (코드 $code)"
       is java.net.UnknownHostException,
       is java.net.SocketTimeoutException,
       is java.io.IOException -> "네트워크 연결을 확인해 주세요."
       else -> "오류가 발생했습니다. (${message ?: "알 수 없음"})"
   }
   ```
2. **`TransitRepository.getBusArrivals`/`getSubwayArrivals`**: catch에서 `emptyList()` 대신
   매핑된 메시지를 담은 예외(`TransitException(message, cause)` 등)를 던지도록 변경.
   → 호출부(`MissionEngine`)가 "0건(정상)"과 "예외(실패)"를 진짜로 구분할 수 있게 됨.
3. **`MissionEngine.fetchArrivals`**: catch 블록에서 위 매핑 유틸을 통해 실제 원인 메시지 노출.
   `results.isEmpty()` 케이스는 예외가 아니므로 그대로 "정상적으로 도착 정보 없음" 문구 유지.
4. **운행종료 세분화**: `toTransitArrival()`이 걸러낸 원본 메시지 중 `"운행종료"` 포함 개수를
   `TransitRepository` 레벨에서 함께 반환(또는 별도 플래그)하면, `MissionEngine`이 "전부
   운행종료라 없음"과 "그냥 아직 안 옴"을 구분해 다른 문구를 띄울 수 있음. 첫차/막차 API
   연동(`TODO.md`의 별도 항목) 없이도 가능한 간단한 개선.

## 범위 밖 (이번 정리에 포함하지 않음)

- `nearbyBusStops`/`searchBusStops`/`getRoutesAtStop` (설정 화면, `BusSelectScreen`/
  `LocationPickerScreen` 쪽) — 이미 로그 + 빈 리스트로 동작 중이라 우선순위 낮음. Phase 2에서
  위 매핑 유틸이 만들어지면 그대로 재사용만 하면 됨.
- 첫차/막차 시각 API 연동 (`TODO.md`에 이미 별도 항목으로 존재) — "운행종료" 세분화는 그 항목의
  간이 버전 정도로만 다룸.
