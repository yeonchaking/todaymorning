# 엣지케이스 — 케이스 정리

> 2026-07-06 세션에서 조사·합의. **실제 코드 변경은 Phase 2로 미룸.** 이 문서는 다음 세션이
> 바로 착수할 수 있도록 조사 결과·수정 방향을 남겨둔 것.

---

## 배경

`TODO.md` 1번 항목: "미션 미설정 상태로 알람, 자정 넘김, 목표 시각 직전 진입 등." 세 가지
하위 케이스를 각각 코드로 확인한 결과, **하나는 실제 버그, 두 개는 이미 기존 로직으로 커버됨**.

---

## 1. 자정 넘김 — 실제 버그, Phase 2에서 수정 필요

**위치**: `mission/MissionEngine.kt` `startCountdown()` (L115-142)

```kotlin
val target = Calendar.getInstance().apply {
    set(Calendar.HOUR_OF_DAY, s.targetHour)
    set(Calendar.MINUTE, s.targetMinute)
    set(Calendar.SECOND, 0)
    set(Calendar.MILLISECOND, 0)
}.timeInMillis
```

목표 시각을 **항상 "오늘 날짜"** 기준으로만 계산한다. 알람 시각과 목표 시각이 자정을
사이에 두고 걸쳐 있는 조합(예: 알람 23:50, 목표 00:10 — 심야 근무자 등)이면:

- 미션이 시작되는 시점(지금 ≈ 23:50)에 `target`은 "오늘 00:10"으로 계산됨.
- `target - now` ≈ **-23시간 40분**으로 이미 크게 지나버린 값이 되어, 화면이 열리자마자
  "시간이 지났어요"로 표시되고 곧바로 수동 성공/실패 선택 UI로 넘어가 버린다.
- 참고로 **알람 자체를 재등록하는 로직**(`AlarmScheduler.nextDailyTrigger`, L30-64)은 이미
  "계산된 시각이 지금보다 과거면 하루를 더한다"는 롤오버 처리가 되어 있다. `MissionEngine`의
  목표 시각 계산만 이 처리가 빠져 있어 **일관성이 깨진 상태**.

### 수정 방향

`target`이 "지금"보다 **일정 임계값 이상** 뒤처져 있으면(단순히 몇 분 전이 아니라, 자정을
넘긴 것으로 볼 수 있을 만큼 크게) 하루를 더한다.

```kotlin
val now0 = System.currentTimeMillis()
if (target < now0 - MIDNIGHT_ROLLOVER_THRESHOLD_MS) {
    target += 24 * 60 * 60 * 1000L
}
```

- 임계값 후보: **3시간**. 일반적인 "알람 → 목표 시각" 간격은 보통 1~2시간 이내이므로,
  목표가 지금보다 3시간 넘게 과거로 계산됐다면 "오늘 이미 지난 것"이 아니라 "자정 넘겨 내일
  도래할 시각"으로 보는 게 합리적.
- **주의**: 이 임계값을 너무 작게 잡으면 아래 3번(목표 시각 직전 진입 — 의도된 동작)까지
  건드려서 정상 케이스를 깨뜨릴 수 있음. 3시간 정도면 안전하게 분리됨.

---

## 2. 미션 미설정 상태로 알람 — 이미 정상 동작, 변경 불필요

**확인한 코드 흐름**:
- `NavGraph.kt`: 알람으로 열리면 `hasMissionTarget` 여부와 무관하게 항상 `Routes.TIME_ATTACK`으로
  이동 (L43-51).
- `MissionEngine.fetchArrivals()` (L159-199): `!s.hasMissionTarget`이면 즉시
  `"설정에서 출근 경로를 먼저 탐색해 주세요."` 안내를 띄우고 네트워크 호출 없이 리턴.
- `TimeAttackScreen.kt`: `missionState`가 `Active`인 한 하단 액션("탑승 완료" 버튼 또는
  성공/실패 수동 선택)은 노선 유무와 무관하게 항상 노출됨 — 사용자가 노선 없이도 수동으로
  성공/실패를 기록하거나, 목표 시각이 지나면 `MissionFailReceiver`가 자동 실패 처리.

즉 "미션이 설정 안 된 채 알람이 울리는" 상황도 화면이 깨지거나 멈추지 않고, 안내 문구 +
수동 성공/실패라는 기존 설계로 이미 커버되고 있음. **코드 변경 없음.**

(참고: 향후 개선하고 싶다면 "설정으로 이동" 버튼을 타임어택 화면에 추가하는 정도가 가능하지만,
이번 조사 범위에서는 필수로 보지 않음 — 필요해지면 `TODO.md` 새 항목으로 등록.)

---

## 3. 목표 시각 직전 진입 — 이미 정상 동작, 변경 불필요

목표 시각을 살짝 지난 뒤 화면에 진입하거나(수동으로 타임어택 화면을 다시 열람 등), 미션이
진행되던 도중 목표 시각을 통과하는 경우:

- `MissionEngine.startCountdown()`: `remaining <= 0`이 되면 카운트다운 루프만 멈추고
  (`markFinished()`는 호출하지 않음 — 2026-07-06 세션에서 이미 수정된 부분), 폴링·TTS·서비스는
  계속 살아있음.
- `TimeAttackScreen.kt`: `remainingSeconds <= 0`이면 하단 액션이 "탑승 완료" 단일 버튼에서
  "성공/실패 수동 선택" 2버튼으로 자연스럽게 전환됨 (L264-296의 `when` 분기).

목표 시각을 살짝 넘겨 도착하는 차편도 "타면 성공"으로 인정하는 게 이 앱의 설계 의도라서, 이
동작 자체가 버그가 아니라 **의도된 기능**. **코드 변경 없음.**

---

## Phase 2 액션 아이템 요약

- [ ] `MissionEngine.startCountdown()`에 자정 넘김 롤오버 로직 추가 (임계값 3시간, 위 코드 참고)
- [ ] (선택) 미션 미설정 상태에서 타임어택 화면에 "설정으로 이동" 버튼 추가 — 필요성 재검토 후 결정
