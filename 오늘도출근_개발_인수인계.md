# 오늘도출근 앱 개발 인수인계 문서

> 이 문서는 새 Claude 세션이 **이전 세션의 컨텍스트 없이도** 바로 작업을 이어갈 수 있도록 작성된 인수인계서다.

---

## 1. 프로젝트 위치 및 기본 정보

| 항목 | 내용 |
|---|---|
| **Android 프로젝트** | `C:\Users\AM11D\AndroidStudioProjects\todaymorning` |
| **패키지명** | `com.yeon.todaymorning` |
| **앱 이름** | 오늘도출근 |
| **설계 문서** | `C:\Users\AM11D\Documents\NockChaWang\기상미션 앱.md` |
| **개발 계획서** | `C:\Users\AM11D\Documents\NockChaWang\기상미션 앱 개발계획서.md` |
| **참고 코드 (패키지 오류 있음)** | `C:\Users\AM11D\Documents\NockChaWang\TodayCommute_Phase1\` |

> ⚠️ `TodayCommute_Phase1/` 폴더의 코드는 패키지명이 `com.yeon.todaycommute`로 잘못되어 있다.  
> 실제 프로젝트 패키지는 **`com.yeon.todaymorning`** — 모든 파일에 이 패키지를 사용할 것.

---

## 2. 앱 개요 (한 줄 요약)

알람이 울리면 타임어택 화면이 열리고, 실시간 버스/지하철 도착 정보를 보며 "탑승 완료" 버튼을 목표 시각 전에 누르면 성공 — 매일 출근 습관을 게임처럼 쌓는 앱.

---

## 3. 현재 프로젝트 상태

### ✅ Phase 1 완료 (2026-04-20)

| 파일 | 상태 |
|---|---|
| `gradle/libs.versions.toml` | 전체 의존성 추가 완료 |
| `build.gradle.kts` (루트) | kotlin-compose, hilt, ksp 플러그인 추가 완료 |
| `app/build.gradle.kts` | Hilt/KSP/Room/DataStore/Retrofit 의존성 추가, Java 17, BuildConfig 활성화 완료 |
| `gradle.properties` | `android.disallowKotlinSourceSets=false` 추가 완료 |
| `AndroidManifest.xml` | 권한, TodayCommuteApp, AlarmReceiver, MissionFailReceiver 등록 완료 |
| `res/xml/network_security_config.xml` | 버스/지하철 API HTTP 허용 완료 |
| `TodayCommuteApp.kt` | `@HiltAndroidApp` 완료 |
| `alarm/AlarmScheduler.kt` | AlarmManager 래퍼 완료 |
| `alarm/AlarmReceiver.kt` | BroadcastReceiver 완료 |
| `alarm/MissionFailReceiver.kt` | 자동 실패 처리 (Phase 2에서 ViewModel 연동 예정) |
| `data/db/MissionRecord.kt` | Room Entity 완료 |
| `data/db/MissionDao.kt` | Room DAO 완료 |
| `data/db/AppDatabase.kt` | RoomDatabase 완료 |
| `data/datastore/UserSettingsDataStore.kt` | DataStore 래퍼 완료 |
| `di/DatabaseModule.kt` | Hilt 모듈 완료 |
| `domain/model/MissionState.kt` | sealed class 완료 |
| `domain/model/UserSettings.kt` | data class + TransitType enum 완료 |
| `domain/model/TransitArrival.kt` | 버스/지하철 통합 도착 정보 모델 완료 |
| `ui/MainActivity.kt` | `@AndroidEntryPoint`, 5초 알람 테스트 화면 완료 |
| `ui/theme/Color.kt` | 블루-화이트 팔레트 완료 |
| `ui/theme/Theme.kt` | `TodayCommuteTheme` 완료 |
| `ui/theme/Type.kt` | `TodayCommuteTypography` 완료 |

### ❌ Phase 2 — 다음 작업 (설정 화면 + DataStore)

| 작업 | 설명 |
|---|---|
| `SettingsScreen.kt` | 알람 시각, 목표 시각, 대중교통 유형/정류장 설정 화면 |
| `SettingsViewModel.kt` | UserSettingsDataStore 연동 ViewModel |
| `NavGraph.kt` | Navigation Compose 라우팅 구성 |
| `MissionFailReceiver.kt` | ViewModel 연동 — 목표 시각 초과 시 자동 실패 기록 |
| 알람 복원 | 부팅 후 AlarmReceiver에서 AlarmScheduler 재등록 |

---

## 4. 기술 스택

| 레이어 | 기술 |
|---|---|
| 언어 | Kotlin |
| UI | Jetpack Compose + Material 3 |
| DI | Hilt 2.59.2 + KSP |
| DB | Room 2.8.4 |
| 설정 | DataStore Preferences 1.1.1 |
| 네트워크 | Retrofit 2.11.0 + OkHttp 4.12.0 |
| 알람 | AlarmManager (`setAlarmClock`) |
| 아키텍처 | MVVM (ViewModel + Repository) |

**현재 프로젝트 버전 정보:**
- AGP: `9.0.1`
- Kotlin: `2.0.21` (AGP 9 내장 Kotlin 사용 — `kotlin.android` 플러그인 별도 추가 불필요)
- KSP: `2.0.21-1.0.26`
- Gradle: `9.2.1`

---

## 5. 주요 주의사항

### ⚠️ AGP 9.0.1 + Kotlin 호환성 이슈 (Phase 1에서 해결한 것들)

AGP 9.0.1은 Kotlin을 내장(built-in)으로 처리한다. 이로 인해 기존 방식과 다른 설정이 필요하다.

#### kotlin.android 플러그인 — 추가하면 안 됨
AGP 9.0.1은 `kotlin.android` 플러그인을 내부적으로 자동 적용한다.  
`build.gradle.kts` (루트 및 app)에 **`kotlin.android`를 추가하면 `kotlin extension 중복 오류` 발생**.

루트 `build.gradle.kts`:
```kotlin
plugins {
    alias(libs.plugins.android.application) apply false
    alias(libs.plugins.kotlin.compose) apply false
    alias(libs.plugins.hilt.android) apply false
    alias(libs.plugins.ksp) apply false
    // kotlin.android는 추가하지 말 것!
}
```

app `build.gradle.kts`:
```kotlin
plugins {
    alias(libs.plugins.android.application)
    alias(libs.plugins.kotlin.compose)
    alias(libs.plugins.hilt.android)
    alias(libs.plugins.ksp)
    // kotlin.android는 추가하지 말 것!
}
```

#### kotlinOptions 사용 불가 → kotlin { jvmToolchain() } 사용
`kotlin.android` 플러그인이 없으면 `kotlinOptions { jvmTarget = "17" }` 사용 불가.  
대신 플러그인 블록 아래 최상위에 아래와 같이 선언:
```kotlin
kotlin {
    jvmToolchain(17)
}
```

#### KSP sourceSets 충돌 → gradle.properties 설정 필요
KSP가 생성 소스를 `kotlin.sourceSets`로 등록하려 할 때 AGP 9 built-in Kotlin 모드가 막는다.  
`gradle.properties`에 아래 줄 추가 필수:
```
android.disallowKotlinSourceSets=false
```

#### Hilt 버전 — 2.59.2 이상 필수
Hilt 2.58 이하는 AGP 9에서 `Android BaseExtension not found` 오류 발생.  
**AGP 9.0.1에서는 반드시 Hilt 2.59.2 이상 사용.**

#### KSP 버전 — 2.0.21-1.0.26 (2.0.21-1.0.29는 존재하지 않음)
Kotlin 2.0.21 기준 KSP 최신 버전은 `2.0.21-1.0.26`.  
`2.0.21-1.0.29`는 존재하지 않으니 사용하지 말 것.

#### Room 버전 — 2.8.4 이상 필수
Room 2.6.1은 최신 Kotlin/KSP 환경에서 `MissionDao_Impl` 생성 코드의 `Continuation` 타입 불일치 오류 발생.  
**Room 2.8.4 이상 사용.**

---

### compileSdk 구문 (AGP 9.0.1 신규 문법)
기존 `compileSdk = 36` 대신 아래 문법을 쓴다. **건드리지 말 것.**
```kotlin
compileSdk {
    version = release(36) {
        minorApiLevel = 1
    }
}
```

### 패키지명 필수 확인
모든 파일 맨 위 `package com.yeon.todaymorning` — `todaycommute` 아님.

### HTTP 허용 필요
서울 버스 API (`ws.bus.go.kr`)와 지하철 API (`swopenapi.seoul.go.kr`)는 HTTP만 지원.  
`res/xml/network_security_config.xml` 생성 후 Manifest에 등록 필수.

### API 키는 나중에
Phase 3에서 필요. 지금은 `local.properties`에 플레이스홀더만 있으면 됨:
```
BUS_API_KEY=
SUBWAY_API_KEY=
```

---

## 6. 현재 실제 작동하는 build.gradle.kts 전문

### 루트 `build.gradle.kts`
```kotlin
plugins {
    alias(libs.plugins.android.application) apply false
    alias(libs.plugins.kotlin.compose) apply false
    alias(libs.plugins.hilt.android) apply false
    alias(libs.plugins.ksp) apply false
}
```

### `app/build.gradle.kts`
```kotlin
plugins {
    alias(libs.plugins.android.application)
    alias(libs.plugins.kotlin.compose)
    alias(libs.plugins.hilt.android)
    alias(libs.plugins.ksp)
}

kotlin {
    jvmToolchain(17)
}

android {
    namespace = "com.yeon.todaymorning"
    compileSdk {
        version = release(36) {
            minorApiLevel = 1
        }
    }

    defaultConfig {
        applicationId = "com.yeon.todaymorning"
        minSdk = 26
        targetSdk = 36
        versionCode = 1
        versionName = "1.0"
        testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            isMinifyEnabled = false
            proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    buildFeatures {
        compose = true
        buildConfig = true
    }
}

dependencies {
    // AndroidX 기본
    implementation(libs.androidx.core.ktx)
    implementation(libs.androidx.lifecycle.runtime.ktx)
    implementation(libs.androidx.activity.compose)
    implementation(platform(libs.androidx.compose.bom))
    implementation(libs.androidx.compose.ui)
    implementation(libs.androidx.compose.ui.graphics)
    implementation(libs.androidx.compose.ui.tooling.preview)
    implementation(libs.androidx.compose.material3)

    // Hilt
    implementation(libs.hilt.android)
    ksp(libs.hilt.compiler)
    implementation(libs.hilt.navigation.compose)

    // Room
    implementation(libs.room.runtime)
    implementation(libs.room.ktx)
    ksp(libs.room.compiler)

    // DataStore
    implementation(libs.datastore.preferences)

    // Retrofit + OkHttp
    implementation(libs.retrofit)
    implementation(libs.retrofit.gson)
    implementation(libs.okhttp)
    implementation(libs.okhttp.logging)

    // Navigation
    implementation(libs.navigation.compose)

    // Lifecycle / ViewModel
    implementation(libs.lifecycle.viewmodel.compose)
    implementation(libs.lifecycle.runtime.compose)

    // Coroutines
    implementation(libs.coroutines.android)

    // Lottie
    implementation(libs.lottie.compose)

    // Test
    testImplementation(libs.junit)
    androidTestImplementation(libs.androidx.junit)
    androidTestImplementation(libs.androidx.espresso.core)
    androidTestImplementation(platform(libs.androidx.compose.bom))
    androidTestImplementation(libs.androidx.compose.ui.test.junit4)
    debugImplementation(libs.androidx.compose.ui.tooling)
    debugImplementation(libs.androidx.compose.ui.test.manifest)
}
```

---

## 7. Phase 1 체크포인트

- [x] Gradle Sync 오류 없음
- [x] 빌드 성공
- [x] 앱 실행 → 테스트 화면 표시됨
- [x] "5초 후 알람 테스트" 버튼 클릭 → 5초 후 알람 울림
- [x] 알람 울린 후 MainActivity가 다시 열리고 "알람 수신 성공!" 메시지 표시

**✅ Phase 1 완료 → Phase 2 (설정 화면 + DataStore) 진행**

---

## 8. 전체 Phase 로드맵

| Phase | 목표 | 핵심 작업 | 상태 |
|---|---|---|---|
| **Phase 1** | 알람 울림 + MainActivity 열림 | Gradle 세팅, Room, DataStore, AlarmManager | ✅ 완료 |
| **Phase 2** | 설정 화면 — 알람/목표 시각/대중교통 저장 | SettingsScreen, SettingsViewModel, NavGraph | ← 지금 여기 |
| Phase 3 | 타임어택 화면 — 실시간 버스/지하철 표시 | Retrofit API 연동, TimeAttackScreen | |
| Phase 4 | 탑승 성공/실패 기록 + 메인 화면 통계 | MissionRepository, MainScreen | |
| Phase 5 | 게이미피케이션 + 추천 로직 | Lottie 애니메이션, 스트릭, 레벨 | |
| Phase 6 | 안정화 + 배포 | 엣지케이스, ProGuard, Play Store | |
