# 구글 플레이 콘솔 업로드 준비 (오늘도출근)

## 🚀 릴리즈 확정 작업 리스트 (2026-07-10 확정, 우선순위순)

**지금 바로 (리드타임 병목)**

- [ ] 1. 클로즈드 테스트 테스터 모집 시작 — 12명 × 14일 연속 옵트인 게이트
- [ ] 2. 릴리즈 서명 keystore 생성 + minify/난독화 확인

**코드 작업**

- [ ] 3. 막차/첫차 계산 → 실시간 도착정보 반영
- [ ] 4. 오류·예외처리 표준화 + 자정 넘김 롤오버 버그 수정
- [ ] 5. 온보딩/첫 실행 화면
- [ ] 6. 플로팅 위젯 구현 주석처리 (기능 비활성화, Manifest `SYSTEM_ALERT_WINDOW` 권한 제거 포함)
- [x] 7. 잠재리스크 문서 생성 → "성공 시 자동실패 취소 주의사항" 이동, TODO에서 제거 — 완료(2026-07-10, `잠재리스크(오늘도출근).md`)

**제출 서류 (병행)**

- [ ] 8. 개인정보처리방침 URL
- [ ] 9. 민감 권한 선언서 (위젯 비활성화로 `SYSTEM_ALERT_WINDOW` 선언 제외)
- [ ] 10. 스토어 등록정보 — 스크린샷, 설명, 피처 그래픽
- [ ] 11. 프로덕션 신청 질문 응답 — 클로즈드 테스트 종료 후

> P2로 이동: 지하철 미션 직접선택 화면, 플로팅 위젯 재활성화(+전면 숨기기) / P3: 신호등 정밀화
> 완료: 개발자 계정 등록, 트래픽 증설

---

> 2026-07-06 세션에서 정리. "앱 내에서 해야 할 것"과 "외부(Play Console/계정)에서 해야 할 것"을 나눠서 본다.
> 살아있는 작업 목록은 `TODO.md`가 단일 출처이고, 이 문서는 **업로드 준비 관점**에서 다시 정리한 것 + 등록 시 실제로 쓸 문구/체크리스트를 담는다.

---

## 1. 현재 상태

- Phase 2(설정 화면) 진행 중. 알람·타임어택·TTS·알람음/진동 선택·백그라운드 미션 서비스는 구현 완료.
- `targetSdk 36` — Google Play의 신규 앱 타겟 API 요건(Android 15 / API 35 이상, 2025-08-31부터 시행)은 이미 충족.
- 2026-07-06 세션에서 처리 완료: 앱 이름 한글화, 스플래시 화면, 상태별 UI 일관성(로딩/에러/빈상태), OkHttp 릴리즈 로깅 레벨.
- 아직 남은 것: 지하철 미션 직접선택 화면, 릴리즈 서명 keystore, 개인정보처리방침, 스토어 등록정보 등 (아래 표 참고).

---

## 2. 앱 내에서 해야 할 것 (코드)

### 완료 (2026-07-06 세션)

| 항목 | 내용 |
|---|---|
| 앱 이름 한글화 | `strings.xml` `app_name` → "오늘도출근" |
| 스플래시 화면 | `core-splashscreen` 도입, `splash_icon.xml` 하나만 교체하면 로고 변경 가능한 구조 |
| 상태별 UI 일관성 | RouteSelect/BusSelect(노선 바텀시트)/LocationPicker(주소검색)의 로딩·에러·빈상태를 `AppTheme.colors` 기반 공통 컴포넌트(`ui/common/StateViews.kt`)로 통일 |
| OkHttp 로깅 레벨 | 릴리즈 빌드는 BASIC(디버그만 BODY) — API 키·응답 본문이 릴리즈 로그캣에 노출되지 않음 |

### 남은 것 (`TODO.md` 참고, 우선순위순)

1. **지하철 미션 직접선택 화면** (1.0 필수) — 다음 세션
2. **막차/첫차 계산 반영** — 다음 세션
3. **오류·예외 처리 표준화** / **엣지케이스 처리** — 간단하게라도 처리하기로 함, 다음 세션 우선순위
4. **위치 권한 사용 근거 문서화** — 코드 변경 없음, 아래 4번 문서에 반영하면 끝
5. **온보딩/첫 실행 화면** — 필요성은 확인됨, 범위/일정 미정
6. P3(낮은 우선순위): 타임어택 신호등 정밀화, 플로팅 위젯 전면 숨기기

---

## 3. 외부에서 해야 할 것 (Play Console / 계정)

- **Google Play 개발자 계정 등록** ($25 1회, 아직이면)
- **비공개 테스트(Closed testing) 운영** — 2023년 11월 이후 신규 개인 계정은 **테스터 12명 이상이 14일 연속 옵트인** 상태를 채워야 프로덕션 신청 가능. 리드타임이 가장 긴 항목이라 최우선 착수 권장.
- **개인정보처리방침 URL** — 알람·위치 권한 사용하므로 필수
- **민감 권한 선언서** — 아래 5번 참고
- **스토어 등록정보** — 앱 설명, 스크린샷, 피처 그래픽, 카테고리
- **프로덕션 신청 시 질문 응답** — 클로즈드 테스트에서 뭘 배웠는지, 왜 준비됐다고 판단했는지 등 3개 섹션 서술

---

## 4. API 키·트래픽 정책

> **해결됨(2026-07-06): data.go.kr 운영계정 전환 + 트래픽 증설 완료.** 아래는 문제였던 배경 기록.

**구조:** 서버 없이 각 사용자 폰이 공공데이터포털 API(`ws.bus.go.kr`, `swopenapi.seoul.go.kr`)를 직접 호출한다. API 키(`BUS_API_KEY`/`SUBWAY_API_KEY`)는 앱에 내장돼 있어 **모든 사용자가 같은 키를 공유**한다.

**실측 확인한 한도:** data.go.kr 마이페이지(활용신청 상세기능정보)에서 확인 — "서울특별시_버스도착정보조회 서비스"의 세부 기능(`getArrInfoByRouteAllList` 등)은 **기능별로 하루 1,000건**. 앱이 실제로 호출하는 `getStationByUid`(정류장 도착정보 조회)의 정확한 한도는 별도 확인 필요였으나, **운영계정 전환 + 트래픽 증설 완료로 확인 항목 닫음(2026-07-10)**.

**영향 계산:** 미션 중 10초 폴링 기준, 30분 미션 = 하루 약 180콜/인. 클로즈드 테스트 테스터 12명이 동시에 아침 미션을 돌리면 12 × 180 ≈ 2,160콜로 1,000건 한도를 넘길 수 있다 — 테스트 기간 중 "도착정보 없음" 오류가 실제로 뜰 위험.

**대응 방향:**
1. **(권장, 지금)** 클로즈드 테스트 시작 전 data.go.kr에서 해당 기능의 트래픽 증설 신청 — 심의여부가 "자동승인"인 서비스라 절차가 어렵지 않음. 예상 테스터 수·호출 패턴을 사유로 제출.
2. **(장기, 사용자 증가 시)** 자체 서버 캐싱 프록시 도입 — 클라이언트가 공공API를 직접 안 때리고 서버가 5~10초 캐싱 후 여러 사용자에게 같은 응답을 재사용. 트래픽을 수십~수백 배 절약 가능하지만 서버 인프라가 추가로 필요.
3. 폴링 간격을 늘리는 것(예: 10초 → 20~30초)도 트레이드오프로 고려 가능하나 실시간성이 핵심인 앱 특성상 우선순위 낮음.

**1.0 범위 명시:** 개인/소규모 배포로 명시하고, 실사용자가 늘어나는 시점에 마이페이지에서 실제 잔여 트래픽을 재확인.

---

## 5. 민감 권한 / 서비스 타입 — 등록 시 제출용 초안

Play Console 데이터 보안/권한 선언 폼에 넣을 문구 초안. 실제 제출 시 화면 문구에 맞게 다듬을 것.

| 권한/타입 | 실제 용도 | 제출 문구 초안 |
|---|---|---|
| `SCHEDULE_EXACT_ALARM`/`USE_EXACT_ALARM` | 출근 알람을 정확한 시각에 울리기 위함 | "사용자가 설정한 출근 알람을 정확한 시각에 울리기 위해 사용합니다." |
| `USE_FULL_SCREEN_INTENT` | 알람 화면을 잠금 화면 위에 즉시 표시 | "알람이 울릴 때 잠금 화면 위에 알람/미션 화면을 즉시 표시하기 위해 사용합니다." |
| `FOREGROUND_SERVICE_MEDIA_PLAYBACK`(`AlarmRingService`) | 알람음 재생 | "알람이 울리는 동안 알람음을 재생하는 포그라운드 서비스입니다." — **유지 결정**(2026-07-06), mediaPlayback 정의에 부합 |
| `FOREGROUND_SERVICE_DATA_SYNC`(`MissionService`) | 미션 진행 중 실시간 버스/지하철 도착정보를 주기적으로 조회 | "출근 미션 진행 중 서버(공공데이터 API)에서 실시간 도착정보를 주기적으로 가져와 사용자에게 보여주기 위한 서비스입니다." — **유지 결정**(2026-07-06), Android 15 dataSync 하루 6시간 제한은 실사용 패턴(하루 수십 분)에 문제없음 |
| `SYSTEM_ALERT_WINDOW` | 플로팅 위젯(미션 진행 중 다른 앱 위에 도착정보 표시) | "미션 진행 중 다른 앱을 사용하면서도 실시간 도착정보를 확인할 수 있도록 화면 위에 작은 위젯을 표시하는 데 사용합니다." — **앱 등록 시점에 다시 체크** |
| `ACCESS_FINE_LOCATION`/`ACCESS_COARSE_LOCATION` | 버스 정류장 선택 화면에서 "내 위치로 이동" | "출근 경로 설정 시 지도에서 내 위치 주변 정류장을 쉽게 찾도록 사용하며, 위치 정보를 저장하거나 서버로 전송하지 않습니다." |

---

## 6. 추천 순서

1. **지금 바로**: 클로즈드 테스트 테스터 모집 시작 (14일 게이트가 병목)
2. 병행: data.go.kr 트래픽 증설 신청, keystore 서명 설정
3. 클로즈드 테스트 기간(14일) 동안: 지하철 화면·예외처리·온보딩 등 남은 기능 마감
4. 개인정보처리방침·권한 선언서(위 5번 표)·스토어 등록정보 준비
5. 14일 + 테스터 12명 충족 후 프로덕션 신청
